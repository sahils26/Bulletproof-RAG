[project]
name = "eval-harness"
version = "0.1.0"
description = "Evaluation framework, scorers, and CI integration"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "pydantic>=2.12.5",
]

[tool.uv.sources]
shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
