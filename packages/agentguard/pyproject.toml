[project]
name = "agentguard"
version = "0.1.0"
description = "Circuit breakers, budgets, tracing, and dashboard API"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "pydantic>=2.12.5",
]

[tool.uv.sources]
shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
