[project]
name = "dashboard"
version = "0.1.0"
description = "Web dashboard for monitoring the RAG pipeline"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "pydantic>=2.12.5",
]

[tool.uv.sources]
shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
